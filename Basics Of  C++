//double with 6 decimal places 

int main() {
	// your code goes here
	double p=0;
	double r = 0;
	double t=0;
	cin>>p;
	cin>>t;
	cin>>r;
	double ans = (p*r*t)/100;
	printf("%.6f",ans); 
	return 0;
}

find prime
Normal for loop gives tle... so we iterate till sqrt of N as num above it will not divide that number.

    int isPrime(int N){
        // code here
        if(N==2)return 1;
        if(N==1)return 0;
        for(int i =2; i<=sqrt(N); i++){
            if(N%i == 0)return 0;
        }
        return 1;
        
    }


floating point error... caused due to division by zero... refer this chat: https://chatgpt.com/share/9e4f8cf5-4e0e-43a6-8312-efb4c2fda17c
class Solution{
public:
    int evenlyDivides(int N){
        //code here
        int num = N;
        int count=0;
        while(num){
            int check = num%10;
            if(check!=0 && N%check == 0){    //here the error is caused as we divide N by check and check can be zero... so we apply if case that check is not zero. 
                count++;
            }
            num=num/10;
        }
        return count;
    }
};

